{
  "name": "agripocket",
  "private": true,
  "type": "module",
  "description": "Root wrapper — app Vite in web/",
  "scripts": {
    "build": "npm run build --prefix web",
    "vercel-build": "npm run build --prefix web"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@supabase/supabase-js": "^2.49.0",
    "@vercel/functions": "^3.6.0",
    "pdf-parse": "^1.1.4"
  }
}
